// Index service - Main services logic

// Import user service
const userService = require('./users');

// Export all services
module.exports = {
  userService,
  // Add more services here if needed
};
